# 新宿（astrbot_plugin_shinjuku）

基于 [AstrBot](https://github.com/Soulter/AstrBot) 的原生上机计费插件，用于新宿音游窝的会员、入场、计费与结算。数据使用 **SQLite 本地存储**
- 插件名：`astrbot_plugin_shinjuku`
- 显示名：新宿
- 版本：v0.1.4
- 最低 AstrBot 版本：>= 4.16

--若有音游窝计费需求，请联系新宿音游社qq群1064813917
## 目录结构

```
astrbot_plugin_shinjuku/
├── main.py               # 插件入口：指令注册、权限、消息格式化
├── shinjuku_service.py   # 业务层：SQLite 存取、计费引擎、欠费、兑换码
├── metadata.yaml         # 插件元数据
├── _conf_schema.json     # 设置表单（AstrBot Web 控制台自动生成配置页）
└── requirements.txt      # 依赖：aiosqlite
```

## 功能特性

- 会员注册：QQ 号自动绑定，支持管理员代注册
- 入场/退场计费：白天/夜晚两档时段、按小时计费、分时段封顶、24 小时逐块封顶
- 月卡（通行证）：月卡用户享受更低的单价与封顶价
- 优惠券：管理员可发 X 折（0–10 折）优惠券，结算自动抵扣，单次使用作废
- 欠费机制：余额不足时仍可退场，差额记为负数余额；欠费期间禁止入场，充值补齐后解除
- 钱包体系：付费余额 / 免费余额 / 优惠券 / 通行证
- 兑换码：支持管理员生成兑换码（可设领取次数），注册礼包可直接配置兑换码自动发放
- 资产流水：所有资产变动写入日志，可追溯
- 店内人数：群内直接发送 `j`（或 `xsj`、`新宿几`、`窝几`、`wj`）查询当前在线人数

## 配置项

设置表单由 `_conf_schema.json` 生成，在 AstrBot Web 控制台的插件配置页中修改。

### 基础配置

| 配置项 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `database_path` | string | 空 | SQLite 数据库文件路径；留空则默认存放在 AstrBot 数据目录 `plugin_data/astrbot_plugin_shinjuku/shinjuku.db`，删除插件不丢数据；也可填绝对路径 |
| `currency` | string | 馕 | 货币显示名 |
| `admins` | list | [] | 管理员用户 ID（QQ 号），可执行管理指令 |
| `redeem_code_on_register` | string | 空 | 注册后自动发放的兑换码；留空注册不送礼 |
| `points_per_amount` | int | 10 | 封顶积分折算金额（元）；触发封顶时每 10 元封顶得 1 积分（向上取整），正常时段按游玩小时数 1 小时 1 积分，设 0 关闭 |

### 计费规则（`billing`）

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `day_start` / `day_end` | 11:30 / 00:00 | 白天时段 |
| `day_price` / `day_price_pass` | 12 / 11 | 白天每小时价格（普通 / 月卡） |
| `day_cap` / `day_cap_pass` | 69 / 59 | 白天封顶（普通 / 月卡） |
| `night_start` / `night_end` | 00:00 / 11:30 | 夜晚时段 |
| `night_price` / `night_price_pass` | 13 / 12 | 夜晚每小时价格（普通 / 月卡） |
| `night_cap` / `night_cap_pass` | 69 / 59 | 夜晚封顶（普通 / 月卡） |
| `cap_24h` / `cap_24h_pass` | 99 / 88 | 每个 24 小时块封顶（普通 / 月卡） |
| `grace_minutes` | 15 | 宽限分钟：零头不足该分钟数不计该小时 |

## 指令

### 普通用户指令

| 指令 | 别名 | 说明 |
|---|---|---|
| `/register` | — | 注册用户 |
| `/login` | `/入场` `/上机` | 入场开始计费 |
| `/logout` | `/出场` `/下机` `/离场` `/退场` | 退场结算 |
| `/billing` | `/账单` `/b` | 查看当前账单 |
| `/wallet` | `/钱包` | 查看钱包 |
| `/items` | `/背包` | 查看资产清单 |
| `/history` | `/历史记录` | 查看历史记录（可加数量，如 `/history 10`） |
| `/redeem` | — | 兑换兑换码，如 `/redeem ABC123` |
| `j` | `xsj` `新宿几` `窝几` `wj` | 直接发送（无斜杠）查询店内人数 |
| `定价表` | — | 直接发送「定价表」（无斜杠）查看当前计费价格 |

### 管理员指令（需在 `admins` 中配置 QQ 号）

| 指令 | 用法 | 说明 |
|---|---|---|
| `/add` | `/add @用户 金额` | 给用户加货币 |
| `/mj` | `/mj @用户 金额` | 扣除用户货币 |
| `/member` | `/member @成员` | 发放 30 天通行证（重复发放自动续期） |
| `/coupon` | `/coupon @用户 折扣 [天数]` | 发放 X 折优惠券（0–10 折，默认 30 天有效） |
| `/giftcode` | `/giftcode 礼包ID 货币数量 次数` | 生成兑换码：礼包内容 + 货币数量，可领次数封顶 |
| `/ahistory` | `/ahistory 用户 [数量]` | 查看指定用户历史 |
| `/list` | — | 列出当前在线用户 |
| `/register @用户` | — | 代注册用户 |

涉及指定用户的指令支持 `@成员` 或直接输入 QQ 号。

每次退场结算自动发放积分：正常时段按游玩小时数 1 小时 1 积分（不足 1 小时不计）；触发封顶时按封顶金额折算（默认每 10 元封顶得 1 积分，向上取整，如白天封顶 69 得 7、会员夜晚封顶 59 得 6、24h 封顶 99 得 10）。可在 `/wallet` 和 `/items` 中查看。

## 计费规则

- **时段**：白天 11:30–00:00，夜晚 00:00–11:30，均按小时计费；
- **取整**：不足一小时按一小时计；零头 ≤ `grace_minutes`（默认 15 分钟）不计该小时（即 15 分钟内免费）；
- **分时段封顶**：每个白天时段/夜晚时段分别封顶（普通 69 / 月卡 59）；
- **24 小时封顶**：从入场时刻起按 24 小时切块，每块单独封顶（普通 99 / 月卡 88），超过 24 小时按块累加；
- **月卡**：持有通行证的用户按月卡单价与封顶计算；
- **优惠券**：结算时自动选择折扣最大的一张（0–10 折），折后价四舍五入到分（两位小数），使用后作废；
- **欠费**：余额不足仍可退场，差额记为负数余额；负余额期间禁止入场，充值到 0 或以上后解除；退场消息会提示"请联系主理人补款"。

## 兑换码与礼包

- 礼包（`Present`）定义礼包内容，兑换码（`Redeem`）绑定礼包并设置生效/过期时间与总领取次数（`maxUseCount`，默认 1 次）；
- 每个用户对带 `oncePerUser` 的礼包限领一次；同一兑换码按总领取次数封顶；
- 管理员可用 `/giftcode` 基于现有礼包生成兑换码（货币数量按参数覆盖，人数封顶按参数设置）；
- 注册礼包：配置 `redeem_code_on_register` 后，新用户注册时自动用该兑换码领取；码无效/用完不会阻塞注册，仅在回复中提示失败原因。

## 数据存储

- 默认数据库：AstrBot 数据目录 `AstrBot/data/plugin_data/astrbot_plugin_shinjuku/shinjuku.db`（通过 `StarTools.get_data_dir()` 获取，删除插件不丢数据；可在配置 `database_path` 指定其他路径）；
- 自动建表：`User`（用户）、`Bind`（平台绑定）、`Session`（会话）、`Asset`（资产定义）、`UserAsset`（持有资产，数量可为负表示欠费）、`UserAssetLog`（资产变动流水）、`Present`（礼包）、`Redeem`（兑换码）、`RedeemRecord`（兑换记录）；
- 备份：直接复制数据库文件即可；数据库采用 WAL 模式，备份前建议关闭插件。

## 更新日志

### 2026-08-13

- `j` 指令增加等价触发词：`xsj`、`新宿几`、`窝几`、`wj`。
- 账单「计费区间」按 24 小时块分组展示：每块显示原价与封顶价（如 `小计: 207 馕 → 封顶 99 馕`），封顶 99 不再只体现在总价里；
- 计费时段统一显示完整日期（如 `08/12 11:30:00 - 08/12 18:13:35`），避免跨 24 小时块被切开的同一时段看起来像重复计费。

### 2026-08-11

- 新增配置化计费规则：白天/夜晚时段价格与封顶、月卡价格、24 小时封顶、宽限分钟；
- 新增欠费机制（允许负余额结算、欠费禁入场、补款提示）；
- 新增管理指令：`/member`（通行证）、`/coupon`（折扣券，单次作废、两位小数）、`/giftcode`（生成兑换码）；
- 新增「定价表」指令：直接发送 `定价表` 即可查看当前白天/夜晚价格与封顶规则；
- 游玩积分规则调整：正常时段按游玩小时数 1 小时 1 积分；触发封顶时按封顶金额折算（默认每 10 元 1 积分向上取整，如白天封顶 69→7、会员夜晚封顶 59→6、24h 封顶 99→10）；
- 退场/账单消息显示 QQ 昵称；欠费时附加"请联系主理人补款"提示；
- `/items` 增加别名 `/背包`；
- `/wallet` 增加别名 `/钱包`；
